// below is automatically imported for us, but we can
// still manually import it if needed
// import 'dart:core';

// just like C++, Kotlin etc. we start with a main function
// void is an optional keyword, since Dart wil infer that automatically
void main() {
  // run with -> dart main.dart
  print("Hello World");
}
